import { FetchCategoryPipe } from './fetch-category.pipe';

describe('FetchCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new FetchCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
