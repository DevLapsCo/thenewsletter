import { FetchProductPipe } from './fetch-product.pipe';

describe('FetchProductPipe', () => {
  it('create an instance', () => {
    const pipe = new FetchProductPipe();
    expect(pipe).toBeTruthy();
  });
});
