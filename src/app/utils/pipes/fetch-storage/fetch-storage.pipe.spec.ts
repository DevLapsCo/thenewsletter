import { FetchStoragePipe } from './fetch-storage.pipe';

describe('FetchStoragePipe', () => {
  it('create an instance', () => {
    const pipe = new FetchStoragePipe();
    expect(pipe).toBeTruthy();
  });
});
